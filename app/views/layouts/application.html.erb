<!DOCTYPE html>
<html>
  <head>
    <title>Blogw</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>

  <body class="bg-dark">
    <div class="body-wrapper d-flex flex-column">
      <%= render partial: 'header' %>

      <%= content_tag(:div, alert, class: 'alert alert-warning mb-0') if alert %>
      <%= content_tag(:div, notice, class: 'alert alert-primary mb-0') if notice %>

      <main class="body-main bg-darker text-light flex-grow-1">
        <%= yield %>
      </main>

      <footer class="body-footer bg-dark text-muted text-center small">
        © 2020 Honda, Jun
      </footer>
    </div>
  </body>
</html>
